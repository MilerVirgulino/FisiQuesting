{
  "rules": {
    ".read": false,
    ".write": false,
    "battles": {
      "$battleId": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['code', 'status', 'players'])"
      }
    }
  }
}
